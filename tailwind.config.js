/** @type {import('tailwindcss').Config} */
export default {
  theme: {
    extend: {
      fontSize: {
        standart_a: 'clamp(12px, 3vw, 24px)',
        standart_but: '50px',
      },
      fontFamily: {
        oddval: ["Oddval", "sans-serif"],
      },
    },
  },
}